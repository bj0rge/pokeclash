const path = require('path');

// This file is loaded by the script sequelize:db:migrate

// eslint-disable-next-line import/no-dynamic-require
module.exports = require(path.join(__dirname, 'config'));
